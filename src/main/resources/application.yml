spring:
  cloud:
    gateway:
      discovery:
        locator:
          enabled: true
      routes:
      - id: microservice-um
        uri:  http://microservico-um:8080
        predicates:
        - Path=/microservico-um/**
  application:
    name: microservice-gateway